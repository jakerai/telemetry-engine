apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer-service
spec:
  template:
    spec:
      containers:
        - name: consumer-service
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "2"
              memory: "2Gi"
