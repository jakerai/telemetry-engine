server:
  port: 8080
  http2:
    enabled: true  # This allows the Gateway to understand the 'PRI' preface

spring:
  application:
    name: api-gateway
  jackson:
    default-property-inclusion: non_null
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
  cloud:
    config:
      enabled: false
    gateway:
      httpclient:
        # This ensures the Gateway talks to Ingestion Service via HTTP/2
        protocol-config:
          http2:
            enabled: true
            use-http2-0-with-prior-knowledge: true
      server:
        webflux:
          routes:
            - id: auth-service
              uri: http://localhost:5000
              predicates:
                - Path=/api/external/v1/auth/**, /oauth2/**
              filters:
                - name: CircuitBreaker
                  args:
                    name: authCircuitBreaker
                    fallbackUri: forward:/fallback/auth
                
            - id: jwt-auth-route
              uri: http://localhost:5000
              predicates:
                - Path=/api/external/v1/users/**, /api/external/v1/query/**
              filters:
                - name: JwtAuth
                - name: CircuitBreaker
                  args:
                    name: jwtCircuitBreaker
                    fallbackUri: forward:/fallback/jwt
             
            - id: api-key-route
              uri: http://localhost:5001
              predicates:
                - Path=/api/v1/ingest/**
              filters:
                - name: ApiKeyAuth
                - name: CircuitBreaker
                  args:
                    name: apiKeyCircuitBreaker
                    fallbackUri: forward:/fallback/apikey
            # WebSocket Route
            - id: websocket-route
              uri: http://localhost:5001
              predicates:
                - Path=/ws/v1/ingest/**
              filters:
                - name: ApiKeyAuth
                - PreserveHostHeader # Ensures headers aren't dropped
              # WebSockets generally don't work well with CircuitBreakers 
              # because they are long-lived, not request-response.

            # gRPC Route
            - id: grpc-route
              uri: http://localhost:9090
              predicates:
                - Path=/com.telemetry.engine.ingestion.protocol.grpc.IngestionService/**
              filters:
                - name: ApiKeyAuth
app:
  security:
    cache:
      type: caffeine # caffe for caffeine cache, redis for redis
    auth:
      service:
        base-url: http://localhost:5000
    

resilience4j:
  circuitbreaker:
    instances:
      authCircuitBreaker:
        slidingWindowSize: 10
        failureRateThreshold: 1
        waitDurationInOpenState: 10s
      jwtCircuitBreaker:
        slidingWindowSize: 5
        failureRateThreshold: 1
      apiKeyCircuitBreaker:
        slidingWindowSize: 20
        failureRateThreshold: 70
  timelimiter:
    instances:
      authCircuitBreaker:
        timeoutDuration: 3s
      jwtCircuitBreaker:
        timeoutDuration: 3s
      apiKeyCircuitBreaker:
        timeoutDuration: 3s
        
        
management:
  endpoint:
    gateway:
      access: read-only
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,gateway,refresh
  tracing:
    sampling:
      # Trace 100% of requests in dev; reduce in prod
      probability: 1.0
  otlp:
    tracing:
      enabled: false
      # OTLP collector endpoint (gRPC)
      #endpoint: http://localhost:4317
    otlp:
    metrics:
      export:
        enabled: false
      

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [${spring.application.name:}, traceId=%X{traceId:-}, spanId=%X{spanId:-}] [%logger{0}.%M:%L] - %msg%n"
  level:
    root: INFO
    com.telemetry.engine: DEBUG
    org.springframework.security: DEBUG
    org.springframework.cloud.gateway: DEBUG
    reactor.netty.http.client: DEBUG
    org.springframework.cloud.gateway.route.RouteDefinitionRouteLocator: TRACE
